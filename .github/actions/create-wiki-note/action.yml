name: 'Create Wiki Release Note'

description: 'Creates a draft release note and updates sidebar'

inputs:
  service_name:
    required: true
    description: "Name of the microservice"
  version:
    required: true
    description: "Version number"
  token:
    required: true
    description: "GitHub Token"
  actor:
    required: true
    description: "User triggering the build"

runs:
  using: "composite"
  steps:
    - name: Create Wiki Page & Rebuild Sidebar
      shell: bash
      env:
        WIKI_REPO: https://${{ inputs.actor }}:${{ inputs.token }}@github.com/${{ github.repository }}.wiki.git
        VERSION: ${{ inputs.version }}
        SERVICE_NAME: ${{ inputs.service_name }}
        USER: ${{ inputs.actor }}
      run: |
        # --- 1. CLONE WIKI ---
        TEMP_DIR="tmp_wiki_${RANDOM}"
        if ! git clone $WIKI_REPO $TEMP_DIR; then
           echo "::error::❌ Wiki Repo not found."
           exit 1
        fi
        cd $TEMP_DIR
        
        # --- 2. CREATE SERVICE FOLDER & NOTE ---
        mkdir -p "$SERVICE_NAME"
        FILENAME="$SERVICE_NAME/${VERSION}.md"
        
        if [ ! -f "$FILENAME" ]; then
          cat > "$FILENAME" << EOF
        # $SERVICE_NAME: $VERSION
        **Owner:** @$USER | **Date:** $(date -u +"%Y-%m-%d")
        
        ## 📝 Changes
        - (Add release notes)
        
        ## 📦 Artifacts
        - Package: \`@semir-intellisync/$SERVICE_NAME@${VERSION}\`
        EOF
        fi

        # --- 3. 🌳 SIDEBAR GENERATOR (FIXED) 🌳 ---
        echo "🔹 Rebuilding Sidebar..."
        
        # Start Sidebar
        {
          echo "## 🧭 Navigation"
          echo ""
          echo "📂 [[Home]]"
          echo ""
          echo "### 📦 Release Notes"
        } > _Sidebar.md
        
        # Loop through every folder (Microservices)
        services=($(find . -maxdepth 1 -type d ! -name '.' ! -name '..' ! -name '.git' ! -name 'Releases' ! -name 'releases' -exec basename {} \; | sort))
        
        for service_dir in "${services[@]}"; do
            # Find all .md files, sort reverse (newest top)
            files=($(ls -r "$service_dir"/*.md 2>/dev/null))
            
            if [ ${#files[@]} -eq 0 ]; then continue; fi
            
            version_count=${#files[@]}
            
            # START COLLAPSIBLE SECTION
            echo "" >> _Sidebar.md
            echo "<details>" >> _Sidebar.md
            echo "<summary><b>$service_dir</b> ($version_count)</summary>" >> _Sidebar.md
            
            # START HTML LIST (This forces the vertical layout)
            echo "<ul>" >> _Sidebar.md
            
            for f in "${files[@]}"; do
                version_name=$(basename "$f" .md)
                
                # Truncate very long version names for the sidebar display
                display_version="$version_name"
                if [ ${#version_name} -gt 25 ]; then
                    display_version="${version_name:0:22}..."
                fi
                
                # HTML LIST ITEM with Wiki Link
                # We use the full path $f for the link, but short name for display
                echo "<li><a href='$f'>$display_version</a></li>" >> _Sidebar.md
            done
            
            # END HTML LIST & SECTION
            echo "</ul>" >> _Sidebar.md
            echo "</details>" >> _Sidebar.md
        done
        
        # --- 4. PUSH CHANGES ---
        git config user.name "GitHub Actions Bot"
        git config user.email "actions@github.com"
        git add .
        
        if git diff --staged --quiet; then
          echo "No changes."
        else
          git commit -m "Update Sidebar for $SERVICE_NAME $VERSION"
          git push origin master
        fi
        
        cd ..
        rm -rf $TEMP_DIR

